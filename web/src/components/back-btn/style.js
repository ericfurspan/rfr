import styled from 'styled-components';

export const StyledButton = styled.button`
  border: 0;
  margin: 0;
  padding: 0;
  background: transparent;
  box-shadow: none;
  font-size: 1rem;

  & svg {
    margin-right: 0.4em;
  }
`;
